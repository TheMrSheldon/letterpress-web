--- a/include/letterpress/parser/parser.hpp
+++ b/include/letterpress/parser/parser.hpp
@@ -91,11 +91,22 @@
 		}

 		size_t lookAheadMatches(const std::regex& regex) noexcept {
-			auto begin = ParserLookAheadIter(0, this);
-			auto end = ParserLookAheadIter();
-			std::match_results<ParserLookAheadIter> m;
-			bool match = std::regex_search(begin, end, m, regex, std::regex_constants::match_continuous);
-			return match ? m.length() : 0;
+			// libc++ regex_search with bidirectional iterators calls std::distance(begin,end)
+			// internally, which iterates until begin==end.  Our end iterator (parser==nullptr)
+			// never compares equal to a live begin iterator, so this loops until the heap is
+			// exhausted.  Work around by snapshotting the lookahead buffer into a std::string
+			// and using the string overload, which uses random-access iterators.
+			static const size_t CHUNK = 4096;
+			for (size_t i = lookAheadBuffer.size(); i < CHUNK; ++i) {
+				char c;
+				stream.read(&c, 1);
+				if (!stream) break;
+				lookAheadBuffer.push_back(c);
+			}
+			std::string view(lookAheadBuffer.begin(), lookAheadBuffer.end());
+			std::smatch m;
+			bool match = std::regex_search(view, m, regex, std::regex_constants::match_continuous);
+			return match ? (size_t)m.length() : 0;
 		}

 		template <class Pred>
